return {
    "saghen/blink.cmp",

    -- dependencies = { 'rafamadriz/friendly-snippets' },

    build = "cargo build --release",

    opts = {
        -- completion = {
        --     menu = {
        --         border = "rounded"
        --     },
        --
        --     documentation = {
        --         window = {
        --             border = "rounded"
        --         }
        --     }
        -- }
    }
}
