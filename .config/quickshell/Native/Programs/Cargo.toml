[workspace]
resolver = "3"
members = [
    "src/sys-monitor",
    "src/wallpaperctl",
    "src/screencap",
]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
